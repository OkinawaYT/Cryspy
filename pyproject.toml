[project]
name = "cryspy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymatgen>=2025.10.7",
]

[project.optional-dependencies]
chgnet = [
    "chgnet>=0.4.0",
]
